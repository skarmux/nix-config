{
  programs.btop = {
    enable = true;
    settings = {
      # Allow for optional transparency effects of the 
      # underlying terminal to shine through
      theme_background = false;
    };
  };
}
